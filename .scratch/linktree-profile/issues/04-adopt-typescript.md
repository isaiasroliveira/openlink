# 04: Adopt TypeScript

**What to build:** Preserve the profile experience while providing strict types for configuration and browser code, and a compiled static output for Vercel.

**Blocked by:** None.

**Acceptance criteria:** Strict type checking passes; existing site checks run against compiled output; deployment includes only public assets; tag and captions remain optional.
