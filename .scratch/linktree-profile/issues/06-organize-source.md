# 06: Organize application source

Move application code under src and separate pages, reusable components, profile configuration, shared types, styles and tests. Keep sharing inside the profile page. Update entry points and documentation while preserving behavior and local profile settings.

Validation: strict type checking, Vite production build and existing component tests.
